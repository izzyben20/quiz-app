const initialState = {
    question_category: '',
    question_difficulty: '',
    question_type: '',
    number_of_questions: 50,
    score: 0
}

const reducer = (state = initialState, actions) => {
    return state
};

export default reducer;